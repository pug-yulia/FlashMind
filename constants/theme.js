// Central design tokens 
// import this instead of hardcoding values in each screen
// (changing a value here updates it across the whole app)

export const colors = {
    primary: '#4361EE',
    dark: '#1a1a2e',
    background: '#fff',
    backgroundSoft: '#f0f4ff',
    textMuted: '#666',
    streakBg: '#FFF3E0',
    streakText: '#E65100',
};

export const radius = {
    button: 12,
    badge: 20,
};

export const font = {
    sm: 13,
    md: 16,
    lg: 24,
    xl: 32,
};